import * as React from 'react';

declare interface ITdfProps {
  name: string;
}

class Tdf extends React.Component<ITdfProps, any> {
  render(): React.ReactNode {
    return <div />;
  }
}

export default Tdf;
